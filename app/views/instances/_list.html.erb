<%= render 'shared/list', rows: tree_rows(instances), title: t('titles.instances.index'), search_path: instances_path, pagination_collection: instances, new_path: (new_instance_path if can? :new, :instances), new_label: t('titles.instances.new'), import_path: (import_instances_path if can? :import, :instances), import_label: t('instances.import_link'), columns: [:no, :name, :type, :norms, :creator, :action], primary_class: Instance, resource_name: :instances %>
