<%= render 'audits/header' %>
<div class="card">
  <div class="card-header p-0 pt-1">
    <ul class="nav nav-tabs" role="tablist">
      <% if can? :do_develop_audit, :products %>
        <li class="nav-item">
          <a class="nav-link" data-bs-toggle="tab" href="#tab_1" role="tab"><%= t('activerecord.attributes.product_log.develop_id') %></a>
        </li>
      <% end %>
      <% if can? :do_flow_audit, :products %>
        <li class="nav-item">
          <a class="nav-link" data-bs-toggle="tab" href="#tab_2" role="tab"><%= t('activerecord.attributes.product_log.flow_id') %></a>
        </li>
      <% end %>
      <% if can? :do_active_audit, :products %>
        <li class="nav-item">
          <a class="nav-link" data-bs-toggle="tab" href="#tab_3" role="tab"><%= t('activerecord.attributes.product_log.active_id') %></a>
        </li>
      <% end %>
    </ul>
  </div>
  <div class="card-body">
    <div class="tab-content">
      <% if can? :do_develop_audit, :products %>
        <div class="tab-pane fade" id="tab_1" role="tabpanel">
          <%= render 'audits/products/apply_products' %>
        </div>
      <% end %>
      <% if can? :do_flow_audit, :products %>
        <div class="tab-pane fade" id="tab_2" role="tabpanel">
          <%= render 'audits/products/develop_products' %>
        </div>
      <% end %>
      <% if can? :do_active_audit, :products %>
        <div class="tab-pane fade" id="tab_3" role="tabpanel">
          <%= render 'audits/products/flow_products' %>
        </div>
      <% end %>
    </div>
  </div>
  <div class="card-footer text-end">
    <%= link_to t('common.back'), audits_path, class: 'btn btn-secondary' %>
  </div>
</div>

<script type="text/javascript">
  $(function(){
    // Bootstrap 5: jQuery's .tab('show') plugin was removed; use the native
    // bootstrap.Tab API to activate the first tab (and its pane) by default.
    var firstTabEl = document.querySelector('.nav-tabs .nav-link');
    if (firstTabEl) {
      new bootstrap.Tab(firstTabEl).show();
    }
  })
  function failed(id){
    if(confirm('<%= t('common.confirm_reject') %>')){
      showSpinner();
      $.ajax({
        type: 'post',
        url: '<%= do_failed_audit_products_path %>',
        data: {id: id},
        dataType: 'script'
      })
    }
  }
</script>
