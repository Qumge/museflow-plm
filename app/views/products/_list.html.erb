<%= render 'shared/list', rows: tree_rows(products), title: t('titles.products.index'), search_path: products_path, pagination_collection: products, new_path: (new_product_path if can? :new, :products), new_label: t('titles.products.new'), import_path: (import_products_path if can? :import, :products), import_label: t('products.import_link'), columns: [:no, :name, :type, :norms, :creator, :action], primary_class: Product, resource_name: :products %>
